package co.mergedev.domain.model

import co.mergedev.domain.model.resource.ResourceList
import co.mergedev.domain.model.resource.ResourceSummary

interface IComicList : ResourceList<ResourceSummary> {
}